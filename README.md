# LaTeX-шаблон экзаменационных билетов

Этот репозиторий содержит готовый к использованию шаблон для автоматической генерации экзаменационных билетов в формате PDF на основе данных из CSV-файла. 
Шаблон полностью настраивается и поддерживает как двух-, так и трёхвопросные билеты с возможностью указания баллов за каждый вопрос.


## 🔧 Требования

Для успешной компиляции шаблона необходимо наличие следующих LaTeX-пакетов:

- `fontenc`, `inputenc`, `babel` — поддержка русского языка и кодировок  
- `amsmath`, `amssymb` — математические символы  
- `graphicx`, `tikz` — графика и линии оформления  
- `tabularray`, `makecell`, `array` — расширенные таблицы  
- `datatool` — работа с CSV-файлами  
- `ifthen` — условные конструкции  
- `geometry` — настройка полей страницы  

> 💡 Рекомендуется использовать **TeX Live 2022 или новее**, в **MiKTeX** тоже должно работать, но не проверялось.


## 📁 Структура проекта

```
exam-template/
├── exam_card.tex          — основной LaTeX-файл
├── questions.csv          — файл с вопросами (пример включён)
└── README.md              — вы читаете его
```


## 📝 Инструкция по использованию

### 1. Подготовка файла с вопросами

Отредактируйте файл `questions.csv`. Каждая строка соответствует одному экзаменационному билету.

- Для **двухвопросных билетов**: укажите два поля в строке  
  Пример:  
  ```csv
  OneQuestion,TwoQuestion,ThreeQuestion
  "Вопрос 1","Вопрос 2"
  ```

- Для **трёхвопросных билетов**: укажите три поля  
  Пример:  
  ```csv
  OneQuestion,TwoQuestion,ThreeQuestion
  "Вопрос 1","Вопрос 2","Вопрос 3"
  ```

> ⚠️ Убедитесь, что количество столбцов в CSV совпадает с количеством вопросов в билете (см. шаг 2).

Формат файла — UTF-8 без BOM, разделитель — запятая (`,`). 
Многострочные или сложные формулы можно вставлять, экранируя запятые при необходимости или оборачивая вопрос в кавычки.

Для удобного редактирования CSV-файла рекомендуется использовать табличные процессоры, такие как 
**Microsoft Excel**, **LibreOffice Calc**, **Apache OpenOffice Calc**, **Google Таблицы** или **OnlyOffice**. 
Эти программы позволяют наглядно вводить и корректировать вопросы по строкам и столбцам, избегая ошибок форматирования.


### 2. Настройка параметров в `exam_card.tex`

Откройте файл `exam_card.tex` и отредактируйте следующие команды в секции **«Настройки по умолчанию»**:

| Команда | Назначение | Пример |
|--------|-----------|--------|
| `\newcommand{\University}{...}` | Полное название вуза | См. шаблон |
| `\newcommand{\Department}{...}` | Название кафедры | `<<Смысла жизни>>` |
| `\newcommand{\HeadOfDepartment}{...}` | ФИО зав. кафедрой | `К.Х.~Хунта` |
| `\newcommand{\Course}{...}` | Название дисциплины | `утешествия по выдуманным мирам` |
| `\newcommand{\Teacher}{...}` | Преподаватель | `Седловой~Л.~И.` |
| `\newcommand{\AcademicYear}{...}` | Учебный год | `2025–2026` |
| `\newcommand{\DecreeNumber}{...}` | Номер протокола/приказа | `01.23-45/6ДСП` |
| `\newcommand{\DecreeDate}{...}` | Дата утверждения | `11.11.2025` |
| `\newcommand{\FileQuestions}{...}` | Имя CSV-файла | `questions.csv` |
| `\newcommand{\NumberOfQuestions}{...}` | Количество вопросов (2 или 3) | `3` |
| `\newcommand{\OneQuestionMark}{...}` | Баллы за 1-й вопрос | `10` |
| `\newcommand{\TwoQuestionMark}{...}` | Баллы за 2-й вопрос | `10` |
| `\newcommand{\ThreeQuestionMark}{...}` | Баллы за 3-й вопрос (если есть) | `10` |
| `\newcommand{\IsPrintTitlePage}{...}` | Печатать титульный лист? (`true`/`false`) | `true` |
| `\newcommand{\IsPrintCropLine}{...}` | Печатать пунктирную линию разреза? (`true`/`false`) | `true` |
| `\newcommand{\RowHeight}{...}` | Высота строки в таблице (мм) | `20` |

> 💡 Используйте `~` для неразрывного пробела между инициалами (например, `Седловой~Л.~И.`).

> 🔗 Имена и названия заимствованы из произведения братьев Стругацких «Понедельник начинается в субботу».


### 3. Компиляция

Выполните компиляцию **дважды** с помощью `pdflatex`:

```bash
pdflatex exam_card.tex
pdflatex exam_card.tex
```

> 🔁 Второй запуск необходим для корректного отображения количества билетов и таблиц.

Результат: файл `exam_card.pdf` с титульным листом (опционально) и всеми билетами.


## ⚠️ Ограничения и известные проблемы

- **Дублирование кода таблицы**: в текущей версии используются два почти идентичных блока `tblr` для 2- и 3-вопросных билетов, так как условные операторы (`\ifthenelse`) плохо работают внутри таблиц `tabularray`. Это техническое ограничение, требующее рефакторинга с использованием `expl3` или другого программного подхода. Пока решения нет.
- **Жёсткая привязка к количеству столбцов**: нельзя использовать разное число вопросов в разных билетах — все билеты должны содержать одинаковое количество вопросов (2 **или** 3).
- **CSV без кавычек**: если вопросы содержат запятые, используйте другой разделитель (например, точку с запятой) и скорректируйте `\DTLloaddb` (требует знания синтаксиса `datatool`).


## 🛠 Планы по улучшению

- [ ] Устранить дублирование кода таблицы с помощью условной генерации строк.
- [ ] Поддержка переменного числа вопросов (например, через циклы в `expl3`).
- [ ] Автоматическая проверка соответствия количества столбцов в CSV и `\NumberOfQuestions`.
- [ ] Поддержка изображений и формул в вопросах (через `\\` и `\(` `\)`).
- [] Возможность оставить в основном файле только глобальные настройки (университет, кафедра, ...), а специфичные для текущей дисциплины настройки (название курса, файл вопросов) выпосить в отдельные файлы. Тогда можно будет на основе одного основного файла генерировать билеты на разные дисциплины.

> 💡  Всех заинтересованных просим высказаться в разделе Issues относительно целесообразности подобных улучшений или предложения новых идей.

---

## 📜 Лицензия

Этот шаблон распространяется по лицензии **[CC0 1.0 Universal (CC0 1.0)](https://creativecommons.org/publicdomain/zero/1.0/){target="_blank"}** — вы можете свободно использовать, изменять и распространять его в любых целях, включая коммерческие, без необходимости указания авторства.

**Примечание:** Лицензия **не распространяется** на имена, названия и другие элементы, заимствованные из произведения братьев Стругацких *«Понедельник начинается в субботу»*. Использование таких элементов допускается исключительно в некоммерческих, образовательных или иллюстративных целях в рамках добросовестного использования (fair use) и с соблюдением авторских прав правообладателей оригинального произведения.
---

## 🙏 Благодарности

Особая благодарность:

- **Дональду Кнуту** — за создание системы \TeX, заложившей основы современной компьютерной верстки;  
- **братьям Стругацким** — за вдохновляющее произведение *«Понедельник начинается в субботу»*, откуда заимствованы названия и имена в примере;  
- **[@vanyabeat](https://github.com/vanyabeat){target="_blank"}** — за первоначальную идею и вдохновение;  
- **всем пользователям и участникам** — за обратную связь, предложения и поддержку развития;  
- а также разработчикам ключевых LaTeX-пакетов, без которых этот шаблон был бы невозможен:  
  - `datatool` — Dr. Nicola Talbot,  
  - `tabularray` — LianTze Lim,  
  - `tikz` — Till Tantau.

Удачи на экзаменах! 🎓
